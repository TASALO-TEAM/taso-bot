# src/utils/ads_manager.py
# Stub: BBAlert expects get_random_ad_text() that returns formatted ad string.
# For minimal integration, return empty string (no ads).
import random

def get_random_ad_text() -> str:
    """Return empty string — no advertisement system in taso-bot."""
    return ""
